import 'package:flutter/material.dart';

class Predictor extends StatelessWidget {
  const Predictor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Predictor Screen'),
    );
  }
}
